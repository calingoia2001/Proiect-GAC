# The application is made using the MATLAB platform, and is made in three files, one main and two for functions.
The interface allows the user to modify the signal parameters, resistors and other parameters.
